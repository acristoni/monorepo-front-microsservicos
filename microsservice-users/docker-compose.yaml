version: '3.1'

services:

  db_user:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: admim
      POSTGRES_DB: user_microservice
    ports:
      - '5432:5432'
      
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080